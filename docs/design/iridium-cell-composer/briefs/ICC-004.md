---
type: brief
id: ICC-004
cluster: iridium-cell-composer
title: Default-on optional syntax for parser-free terminal hosts
---

# ICC-004: Default-on optional syntax for parser-free terminal hosts

> **Cluster:** iridium-cell-composer
> **Depends on:** ICC-003
> **Checklist:**
> - C7 — Default syntax stays enabled while a no-default composer links no parser and preserves cell/editor behavior
> **Stories:**
> - S3 (Norn host integrator, host) — As a host integrator, I can embed the composer in my rectangle while retaining ownership of terminal and focus.
> - S4 (Norn host integrator, host) — As a host integrator, I can adopt the composer with proof that existing renderer and input paths still behave as before.

## Purpose

Resolve the actual tree-sitter native-links collision without migrating Norn/Chiron or adding parser startup/caches to its plain-text composer.

## Task

Preserve the reviewed and locally verified parser-free terminal feature in the authorized private branch checkpoint. The actual Git revision and push verification are recorded externally after commit; no main or venue/Fable landing claim.

## Requirements

### R1: Explicit TUI syntax feature and parser-free rendering

Implement D16. Feature-select only actual syntax dependencies/state/mapping/tests. Use the existing plain Palette style and existing shared geometry/selection/search/caret paint logic when syntax is absent. No unused-argument workarounds, simulated cache or silent fallback. No editor kernel modifications.

**Acceptance:**
- TUI default feature enables syntax; no-default TUI/editor dependency graph contains no iridium-syntax or tree-sitter packages. Optional Rope dependency is selected only by syntax; Frame is zero-sized without it.
- Both complete cargo test --locked -p iridium-tui and --no-default-features pass; all existing public composer/primary-caret fixtures run in both scopes. New feature fixture verifies fixed text styles with/without syntax, language auto-pairs, Unicode paste and history.
- Both corresponding all-targets cargo clippy selections pass with -D warnings; cargo fmt --all -- --check passes. Exact source before/after and log witnesses; no old assembled proof reused as new.
- Existing syntax cache ownership/equal-counter/divergent-clone tests remain present and execute by default. Feature-specific test census is recorded, not called ignored/skipped green.
- Existing Rust syntax benchmark declares required-features syntax; no parser-free Rust timing claim.
- No editor/Norn/Chiron tree-sitter migration, no added mapping authority, no changed default colors or parser behavior. Each production file stays under its registered local size limit; legacy test inventory remains separate.

**Files:**
- create: crates/iridium-tui/tests/syntax_features.rs
- modify: crates/iridium-tui/Cargo.toml
- modify: crates/iridium-tui/src/lib.rs
- modify: crates/iridium-tui/src/frame/mod.rs
- modify: crates/iridium-tui/src/frame/types.rs
- modify: crates/iridium-tui/src/frame/render.rs
- modify: crates/iridium-tui/src/frame/cell_render.rs
- modify: crates/iridium-tui/src/frame/prepared_cells.rs
- modify: crates/iridium-tui/src/frame/palette.rs
- modify: crates/iridium-tui/src/frame/tests.rs
- modify: crates/iridium-tui/src/frame/wrap_tests.rs

**Checklist:**
- C7 — Default syntax stays enabled while a no-default composer links no parser and preserves cell/editor behavior

**Stories:**
- S3 (Norn host integrator, host) — As a host integrator, I can embed the composer in my rectangle while retaining ownership of terminal and focus.
- S4 (Norn host integrator, host) — As a host integrator, I can adopt the composer with proof that existing renderer and input paths still behave as before.

## Boundaries

- Exactly10 modified TUI files and one new public integration test; no kernel source, Cargo.lock version churn or Norn source. Additional paths require a registered amendment.
- Root authorizes dedicated ICC CPU cache jobs2 for both scopes. No GPU/workspace expansion, main, public action or installation. Commit/push only after actual checks.

## Verification

- Render and coverage before source edit.
- Current feature-specific tests/strictClippy/fmt and graph proof before the actual private Git pin.
