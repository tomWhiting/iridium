---
type: brief
id: ICC-003
cluster: iridium-cell-composer
title: Explicit host ownership of primary terminal caret paint
---

# ICC-003: Explicit host ownership of primary terminal caret paint

> **Cluster:** iridium-cell-composer
> **Depends on:** ICC-001, ICC-002
> **Checklist:**
> - C6 — Primary caret paint can be delegated to the host without changing underlay or secondary carets
> **Stories:**
> - S3 (Norn host integrator, host) — As a host integrator, I can embed the composer in my rectangle while retaining ownership of terminal and focus.
> - S4 (Norn host integrator, host) — As a host integrator, I can adopt the composer with proof that existing renderer and input paths still behave as before.

## Purpose

Norn can place its hardware primary cursor without an additional painted primary block or duplicating Iridium selection/search/grapheme rendering.

## Task

Preserve this locally checked and reviewed implementation in the authorized private codex/iridium-cell-transactions checkpoint. Actual revision and push verification belong in the external checkpoint receipt; main merge, public actions and venue/Fable landing claims remain separate.

## Requirements

### R1: Optional primary-caret paint in the existing borrowed render path

Add the explicit method in D15. Existing render_cells delegates true without changing its rendering contract. False skips exactly the primary entry when present, preserves secondary paint regardless of coordinate coincidence, and leaves layout caret hints and all underlay/chrome rendering intact.

**Acceptance:**
- Public backward-selection fixture verifies complete wide-grapheme selection/search underlay remains when primary paint is false; true matches existing render_cells.
- Public trailing-wide-glyph fixture preserves both glyph cells and the logical/trailing-edge primary metadata while omitting its block paint.
- Offscreen primary with visible secondary still paints the first visible caret. Coincident logical/physical secondary caret remains painted; never suppress by position equality.
- Extent mismatch fails before writes for both modes. Prepared geometry, hit results and editor revision/cursor/history stay unchanged.
- rg -n "render_cells_with_primary_caret|skip_primary" crates/iridium-tui/src/frame/cell_render.rs identifies the actual single render path; no second-frame reconstruction.
- CPU-only cargo test --locked -p iridium-tui; cargo clippy --locked -p iridium-tui --all-targets -- -D warnings; fmt --check. Exact source before/after witnesses; no inherited-result reuse after this source delta.

**Files:**
- modify: crates/iridium-tui/src/frame/cell_render.rs
- modify: crates/iridium-tui/tests/cell_composer.rs

**Checklist:**
- C6 — Primary caret paint can be delegated to the host without changing underlay or secondary carets

**Stories:**
- S3 (Norn host integrator, host) — As a host integrator, I can embed the composer in my rectangle while retaining ownership of terminal and focus.
- S4 (Norn host integrator, host) — As a host integrator, I can adopt the composer with proof that existing renderer and input paths still behave as before.

## Boundaries

- Only the two named TUI files; ICC002 kernel bytes and historical proof files remain untouched.
- Root has coordinated CPU-only checks and accepted a private branch commit/push after review. No main, public visibility/history action or broader GPU/workspace build is authorized by this row.

## Verification

- Register/render/coverage before source changes; public API fixtures exercise caller-visible cells and caret hints.
- Run bounded CPU checks after explicit lane coordination and bind receipts to combined source.
