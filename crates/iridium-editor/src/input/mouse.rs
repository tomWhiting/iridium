//! Mouse input handling.

/// Mouse event handler.
#[derive(Debug, Default)]
pub struct MouseHandler {
    // Mouse state will be added during implementation
    _placeholder: (),
}

impl MouseHandler {
    /// Creates a new mouse handler.
    pub const fn new() -> Self {
        Self { _placeholder: () }
    }
}
