//! The desktop app's tests, split by the surface each one exercises.
//!
//! The helpers live in `support`; everything else is a test, grouped so that
//! a failure names the surface it belongs to before you open the file.

mod panels;
mod project;
mod saving;
mod support;
mod tabs;
mod theme;
