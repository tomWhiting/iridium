# Rust
/target/
# Nested target dirs appear when cargo is run from inside a crate directory
**/target/
**/*.rs.bk
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.so
*.dylib
*.dll
*.a
*.lib
*.exe

# Node/npm (for bindings)
node_modules/
*.node
.npm/
package-lock.json

# WASM build
pkg/
*.wasm

# Test/coverage
*.profraw
*.profdata
coverage/
lcov.info
tarpaulin-report.html

# Benchmark results
target/criterion/

# Documentation build
/docs/book/

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# Editor backups
*.bak
*.orig

# napi-rs generated
index.js
index.d.ts
*.node

.claude/current-session.json
.claude/sessions.jsonl

# Worktrees and local session state
.worktrees/
.claude/current-session.json
.claude/sessions.jsonl
.claude/settings1.json
.claude/feedback/

# Example build output
examples/*/dist/

# Grammar build cache
packages/tree-sitter-builder/.cache/

# Built output for the published TypeScript packages. Built by `npm run build`
# (tsconfig.build.json) and shipped in the npm tarball via `files`, never
# committed — the source is the record, the build is derived.
packages/@iridium/core/dist/
packages/@iridium/syntax-worker/dist/
