{
  "name": "@iridium/tree-sitter-builder",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "build:quick": "bun run build.ts --quick",
    "clean": "rm -rf .cache"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
